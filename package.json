{
  "name": "@akkadu/rsi-api-widget",
  "version": "0.0.2",
  "description": "rsi api widget for Akkadu RSI",
  "private": true,
  "main": "index.js",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:serve": "vitepress serve docs",
    "install": "lerna run install",
    "build": "lerna run build",
    "pre-release":"lerna version patch && lerna run build && lerna run install",
    "release": "git fetch --tags && lerna run build && lerna version && lerna publish from-package"
  },
  "author": "Rouiller Romain <rouiller.romain11@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "babel-jest": "^26.6.0",
    "lerna": "^4.0.0",
    "eslint": "^7.11.0",
    "webpack": "4.44.2",
    "rollup": "^2.0.6"
  },
  "dependencies": {
    "@akkadu/rsi-api-interpretation-player": "^0.2.0",
    "tslint": "^6.1.3",
    "typescript": "^4.2.4",
    "rollup-plugin-commonjs": "^10.1.0",
    "rollup-plugin-node-builtins": "^2.1.2",
    "rollup-plugin-node-resolve": "^5.2.0"
  }
}
